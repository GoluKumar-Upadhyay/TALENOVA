"use client";
import { useEffect, useState } from "react";
import { Search, Upload, Trash2 } from "lucide-react";
import { api } from "../../../lib/api";
import { uploadFile } from "../../../lib/upload";
type FileItem={id:string;name:string;url:string;type:string;size:number};
export default function MediaManager(){const [files,setFiles]=useState<FileItem[]>([]);const [folder,setFolder]=useState("images");const [search,setSearch]=useState("");const [error,setError]=useState("");async function load(){try{const data=await api<FileItem[]>(`/storage/files?folder=${folder}&search=${encodeURIComponent(search)}`);setFiles(data)}catch(e){setError(e instanceof Error?e.message:"Storage unavailable")}}useEffect(()=>{load()},[folder]);async function upload(file?:File){if(!file)return;try{await uploadFile(file,folder);load()}catch(e){setError(e instanceof Error?e.message:"Upload failed")}}async function remove(id:string){if(!confirm("Delete this file?"))return;await api(`/storage/files/${id}`,{method:"DELETE"});load()}return <main className="min-h-screen bg-slate-50 p-6 lg:ml-64 md:p-10"><p className="eyebrow">Media manager</p><h1 className="mt-2 text-3xl font-black">Files and uploads</h1><div className="mt-8 flex flex-wrap gap-3"><select value={folder} onChange={e=>setFolder(e.target.value)} className="rounded-xl border bg-white p-3"><option value="images">Images</option><option value="videos">Videos</option><option value="documents">Documents</option></select><div className="flex rounded-xl border bg-white"><Search className="m-3 text-slate-400" size={18}/><input value={search} onChange={e=>setSearch(e.target.value)} onKeyDown={e=>e.key==="Enter"&&load()} placeholder="Search files" className="p-3 outline-none"/></div><label className="flex cursor-pointer items-center gap-2 rounded-xl bg-brand px-4 py-3 font-bold text-white"><Upload size={17}/>Upload<input type="file" className="hidden" onChange={e=>upload(e.target.files?.[0])}/></label></div>{error&&<p className="mt-5 rounded-xl bg-red-50 p-4 text-red-700">{error}</p>}<div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{files.map(file=><article key={file.id} className="overflow-hidden rounded-2xl border bg-white">{file.type.startsWith("image")?<img src={file.url} alt={file.name} className="h-40 w-full object-cover"/>:<div className="flex h-40 items-center justify-center bg-slate-100 text-sm text-slate-500">{file.type}</div>}<div className="flex items-center justify-between p-3"><span className="truncate text-sm font-semibold">{file.name}</span><button onClick={()=>remove(file.id)} className="text-red-600"><Trash2 size={17}/></button></div></article>)}{!files.length&&!error&&<p className="col-span-full rounded-2xl border bg-white p-10 text-slate-500">No files found.</p>}</div></main>}
